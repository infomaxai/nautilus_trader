# Template

A minimal example project combining Python and Rust using [PyO3](https://pyo3.rs/) and [maturin](https://www.maturin.rs/).

Run `make build` to compile the Rust extension and `make test` to run the Python tests.
